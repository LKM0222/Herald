# 리포 루트의 .env — docker compose 가 변수 치환용으로 읽는 파일.
# 서버 애플리케이션 설정은 server/.env 에 따로 있다. 둘은 다른 파일이다.
#
# 이 파일을 리포 루트에 .env 로 복사한다:  cp deploy/.env.example .env

# Caddy 가 인증서를 발급받을 도메인. DNS 가 이 서버를 가리키고 있어야 한다.
HERALD_DOMAIN=herald.example.com
