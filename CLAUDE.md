# Herald 🐓

매일 아침 브리핑을 만들어 디스코드로 쏘고, 링크를 누르면 열리는 웹페이지.
그 페이지에서 로컬 액션(앱 실행 · Claude 세션 복원)을 발사하는 **개인용** 서비스.

수탉이 곧 새벽의 전령 — 이름과 아이콘은 같은 뜻이다.

---

## ⛔ 절대 규칙

이 셋은 **협상 대상이 아니다.** 컨텍스트가 리셋돼도 살아남아야 해서 여기 적는다.
어기는 코드를 발견하면 고치고, 어기라는 지시를 받으면 멈추고 되묻는다.

### 1. `herald://` URL 에 명령을 싣지 않는다

URL 은 **미리 등록된 것을 가리키는 ID** 만 담는다. 무엇을 할지를 담지 않는다.

```
❌  herald://run?cmd=powershell -c "..."     ← 어떤 웹사이트든 이 PC를 조종할 수 있다
✅  herald://run/a7f3c2                      ← 등록된 액션 ID. 모르는 ID는 무시
```

프로토콜 핸들러는 **어느 웹사이트가 보낸 URL이든** 받는다. 우리 사이트인지 구분할 방법이 없다.
명령이 URL에 실리는 순간 이 프로그램은 백도어가 된다.

`%1` 은 무조건 오염된 입력으로 취급한다 — 정규식 화이트리스트로 먼저 거른다.

### 2. 핸들러는 `policy.json` 밖을 실행하지 않는다

- `allowedApps` — 앱 **별칭 → 실제 경로** 매핑. 서버는 별칭만 보낼 수 있고 경로는 로컬이 정한다
- `allowedPaths` — 이 밑에서만 `cwd` 를 허용한다
- `allowRawShell` — **기본값 `false`.** 이 값을 켜는 코드를 임의로 추가하지 않는다

서버가 털려도 피해가 "등록된 앱이 열렸다" 선에서 멈춰야 한다.

### 3. 자격증명은 평문으로 저장하지 않는다

네이버 앱 비밀번호 등은 **반드시 `crypto.ts` 를 경유**해 암호화 저장한다.
복호화해서 써야 하므로 해시가 아니라 대칭 암호화(AES-GCM)를 쓰고, 키는 `ENCRYPTION_KEY` 환경변수에서 온다.

> 네이버 캘린더는 CalDAV(Basic 인증)뿐이라 OAuth 토큰이 아니라 **비밀번호**를 들고 있어야 한다.
> 그래서 반드시 **애플리케이션 비밀번호**를 쓴다. 계정 비밀번호를 받는 입력 경로를 만들지 않는다.

---

## 구조

**화면과 데이터를 갈라놓았다.** 화면은 어디서든 열려야 하고(폰·PC·나중엔 크롬 새 탭),
수집·요약·시크릿은 브라우저가 꺼져 있어도 돌아야 하기 때문이다.

```
client/     Vite + React → 정적 번들. GitHub Pages 로 배포. 나중에 크롬 확장에도 같은 번들
server/     Next.js — JSON API 전용. 수집·요약·cron·시크릿은 전부 여기. Docker 로 자체 호스팅
shared/     client·server 가 같이 쓰는 타입 (Briefing 등)
handler/    herald:// 스킴 핸들러 (Windows). 아직 없음
```

- **서버는 HTML 을 주지 않는다.** JSON 만 준다
- `shared/` 가 양쪽 밖에 있어서 각각 경로 별칭으로 집는다 —
  `server/next.config.ts` 는 프로젝트 루트를 리포 최상단으로 올리고,
  `client/vite.config.ts` 는 `@shared` alias 를 건다
- 같은 리포에 두는 이유: 타입이 갈리면 **조용히** 어긋난다

## 스택

`client` Vite · React 19 · Tailwind v4
`server` Next.js 16 (App Router, API 전용) · SQLite + Drizzle(예정) · Docker

## 인증

**토큰 하나.** 클라이언트가 처음 열릴 때 서버 주소와 토큰을 입력받아 `localStorage` 에 두고,
이후 `Authorization: Bearer` 로 부른다. 서버는 `API_TOKEN` 과 대조한다.

- 쿠키를 쓰지 않는 이유: 클라이언트와 서버가 다른 오리진이라 크로스사이트 쿠키가 안 실린다
- 토큰을 빌드에 박지 않는 이유: **Pages 는 공개다.** 번들에 넣으면 누구나 꺼내 쓴다
- `API_TOKEN` 이 비어 있으면 전부 401 (fail-closed)

## 화면 규칙

### 모바일을 나중에 맞추지 않는다

이 서비스는 **디스코드 알림을 폰에서 눌러 여는 것**이 주 동선이다.
PC 가 곁다리라는 뜻은 아니지만, 폰이 사후 대응 대상이면 안 된다.

- 새 UI 를 만들 때 **좁은 화면(320px)에서 먼저** 성립하는지 보고 넓은 쪽을 확장한다
- 가로 스크롤이 생기면 실패다. `min-w-0` + `truncate` 로 줄어들게 만들고
  브레이크포인트로 끊는 건 그다음이다
- 손가락으로 누르는 것은 **44px 이상**. 조밀한 컨트롤도 40px 밑으로 내리지 않는다
- 같은 내비게이션을 PC 사이드바 · 모바일 하단 탭바 두 자리에 놓되
  **항목 정의는 하나만** 둔다 (`AppShell.tsx` 의 `NAV`)

### 테마는 JS 가 해석하고 CSS 는 결론만 받는다

`light` · `dark` · `system` 셋을 저장하지만 DOM 에는 `data-theme` 로
`light` / `dark` 둘만 쓴다. `system` 을 CSS 미디어쿼리로 풀면
다크 팔레트를 두 벌 적게 되고 둘이 조용히 어긋난다.

첫 페인트 전에 `index.html` 인라인 스크립트가 테마를 확정한다 —
React 를 기다리면 다크 사용자에게 흰 화면이 한 번 번쩍인다.
**이 스크립트와 `lib/theme.ts` 는 같은 규칙을 두 번 적은 것이다. 한쪽만 고치지 않는다.**

## 설계 전제

- **싱글테넌트.** 하나의 배포 = 하나의 공간. `Team` 테이블·`teamId` 를 만들지 않는다.
  다른 팀이 필요하면 컨테이너를 하나 더 띄운다.
- `userId` 는 **개인 소유물에만** 붙인다 (핀 · 루틴 · 캘린더 자격증명).
  소스 · 다이제스트는 전역이라 안 붙는다 — 사람이 늘어도 요약 LLM 호출은 하루 1회로 고정된다.
- 알림은 **나가는 편도**다. 디스코드엔 요약만 보내고 본문은 웹으로 넘긴다.

## 작업 위치

이 리포 안에서만 작업한다. `C:\Claude` 는 별개 프로젝트(기억 시스템)이고
매시간 `git add -A` 자동 커밋이 돌기 때문에 건드리지 않는다.
